# Runtime dependencies
#
# The CLI (github_project_crud.py) has no third-party runtime dependencies.
# It uses only the Python 3.11+ standard library.
#
# MCP server (scripts/mcp_server.py) optional dependencies:
#   pip install "mcp[cli]>=1.0" "python-dotenv>=1.0"
# Or via the package extra:
#   pip install -e ".[mcp]"
